import { StyleSheet } from "react-native";
import { liwiColors } from "../../utils/constants";

export const styles = StyleSheet.create({
  button: { backgroundColor: liwiColors.lighterGreyColor },
  icon: { fontSize: 35, color: liwiColors.redColor },
  container: { marginRight: 70 },
});
