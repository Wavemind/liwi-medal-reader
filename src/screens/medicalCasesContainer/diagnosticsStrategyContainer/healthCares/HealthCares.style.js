import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  spaceText: {
    marginBottom: 20,
    marginLeft:20,
  },
  desc: {
    marginBottom: 10,
    marginLeft:40,
  },
  padded: {
    padding: 20,
  },
});
